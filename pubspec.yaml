name: rise_ui
description: RiseUI — a minimal, HeroUI-inspired Flutter component library.
version: 0.1.1
repository: https://github.com/talhademirbas/riseUI
issue_tracker: https://github.com/talhademirbas/riseUI/issues
homepage: https://github.com/talhademirbas/riseUI

topics:
  - ui
  - widgets

environment:
  sdk: ^3.10.1
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  path_drawing: ^1.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
