# RiseUI example

Showcase app for the [`rise_ui`](https://pub.dev/packages/rise_ui) package. Lists every component and opens one screen at a time for performance.

## Run

```bash
cd example
flutter pub get
flutter run
```

On web, the docs site can embed a single preview via query params (see `lib/main.dart`).
